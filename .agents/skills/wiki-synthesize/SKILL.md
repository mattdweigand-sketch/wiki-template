---
name: wiki-synthesize
description: Run the wiki synthesis workflow. Use when the user says $wiki-synthesize, wiki-synthesize, synthesize the wiki, run synthesis, or wants a corpus distillation pass.
---

# Wiki Synthesize

Run `wiki-synthesize` through the canonical wiki workflow for this repo. Read `AGENTS.md`, then `CONTEXT.md`, then `workflows/maintenance/CONTEXT.md`, then `workflows/maintenance/synthesize.md`, and follow the routed Load / Skip list exactly.
This wrapper is generated from `scripts/wiki-wrapper-contract.json`; canonical behavior lives in `workflows/`.
