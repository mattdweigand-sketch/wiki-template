---
name: wiki-eval
description: Run the wiki tooling eval workflow. Use when the user says $wiki-eval, wiki-eval, run wiki evals, run the live evals, verify wiki guardrails, or wants to check that the wiki tools still work.
---

# Wiki Eval

Run `wiki-eval` through the canonical wiki workflow for this repo. Read `AGENTS.md`, then `CONTEXT.md`, then `workflows/maintenance/CONTEXT.md`, then `workflows/maintenance/eval.md`, and follow the routed Load / Skip list exactly.
This wrapper is generated from `scripts/wiki-wrapper-contract.json`; canonical behavior lives in `workflows/`.
