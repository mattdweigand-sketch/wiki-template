---
description: Configure wiki/domain.md and offer optional GitHub and private-backup connection help
---

Run `wiki-setup` through the canonical wiki workflow. Read `AGENTS.md`, then `CONTEXT.md`, then `workflows/maintenance/CONTEXT.md`, then `workflows/maintenance/setup.md`, and follow the routed Load / Skip list exactly.
This wrapper is generated from `scripts/wiki-wrapper-contract.json`; canonical behavior lives in `workflows/`.

$ARGUMENTS
