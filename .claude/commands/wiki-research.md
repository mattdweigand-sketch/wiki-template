---
description: Run manually invoked wiki research with claim-level independent verification
---

Run `wiki-research` through the canonical wiki workflow. Read `AGENTS.md`, then `CONTEXT.md`, then `workflows/research/CONTEXT.md`, then `workflows/research/research.md`, and follow the routed Load / Skip list exactly.
This wrapper is generated from `scripts/wiki-wrapper-contract.json`; canonical behavior lives in `workflows/`.

$ARGUMENTS
