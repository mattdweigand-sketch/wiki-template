---
description: Answer an ordinary question from the smallest relevant part of the wiki
---

Run `wiki-ask` through the canonical wiki workflow. Read `AGENTS.md`, then `CONTEXT.md`, then `workflows/research/CONTEXT.md`, then `workflows/research/ask.md`, and follow the routed Load / Skip list exactly.
This wrapper is generated from `scripts/wiki-wrapper-contract.json`; canonical behavior lives in `workflows/`.

$ARGUMENTS
